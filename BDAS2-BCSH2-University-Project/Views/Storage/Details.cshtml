@model Storage

@{
    ViewData["Title"] = "Details";
}

<div>
    <h4>@ViewData["Title"]</h4>
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">
            <strong>Error:</strong> @TempData["Error"]
        </div>
    }
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NumberOfShelves)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NumberOfShelves)
        </dd>
    </dl>
    <h5>Products</h5>
    <ul>
        @foreach (var storagedProduct in Model.Products)
        {
            <li>@storagedProduct.Name</li>
        }
    </ul>
</div>
<div>
    <div class="button-row">
        <a asp-action="Save" asp-route-id="@Model.Id" class="btn btn-secondary">Edit</a>
        <a asp-action="Order" asp-route-id="@Model.Id" class="btn btn-secondary">Order</a>

        @if (User.IsInRole(UserRole.Admin.ToStringValue()))
        {
            <form asp-action="Delete" asp-route-id="@Model.Id">
                <input type="submit" value="Delete" class="btn btn-danger" />
            </form>
        }
    </div>
    <div style="margin-top: 10px;">
        <a asp-action="Index" class="btn btn-secondary btn-sm">Back</a>
    </div>
</div>
